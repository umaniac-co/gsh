name: verification

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  native-matrix:
    defaults:
      run:
        shell: bash
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-15
            cc: clang
            sanitizer: true
            libfuzzer: false
            label: macos-arm64-clang
          - os: ubuntu-24.04
            cc: clang
            sanitizer: true
            libfuzzer: true
            label: linux-x86_64-clang
          - os: ubuntu-24.04
            cc: gcc
            sanitizer: false
            libfuzzer: false
            label: linux-x86_64-gcc

    runs-on: ${{ matrix.os }}
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies on Linux
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libsodium-dev pkg-config zsh

      - name: Install dependencies on macOS
        if: runner.os == 'macOS'
        run: brew install bash libsodium zsh

      - name: Strict build and black-box gates
        run: |
          set -o pipefail
          make clean
          mkdir -p build/evidence
          make CC=${{ matrix.cc }} policy conformance check check-variables check-positionals check-background check-config check-aliases check-functions check-fault check-resource 2>&1 | tee build/evidence/gates.txt
          make CC=${{ matrix.cc }} fuzz-smoke FUZZ_CASES=250000 2>&1 | tee -a build/evidence/gates.txt
          make CC=${{ matrix.cc }} fuzz-pty PTY_FUZZ_CASES=5000 2>&1 | tee -a build/evidence/gates.txt

      - name: Clang static analyzer
        if: matrix.cc == 'clang'
        run: |
          set -o pipefail
          make analyze 2>&1 | tee -a build/evidence/gates.txt

      - name: Sanitizer gates
        if: matrix.sanitizer
        run: |
          set -o pipefail
          make check-sanitize 2>&1 | tee -a build/evidence/gates.txt
          ASAN_OPTIONS=abort_on_error=1 UBSAN_OPTIONS=halt_on_error=1 \
            ./build/posix-conformance "$PWD/build/gsh-sanitize" "$(command -v bash)" 2>&1 | tee -a build/evidence/gates.txt
          make fuzz-sanitize FUZZ_CASES=250000 2>&1 | tee -a build/evidence/gates.txt
          make fuzz-pty-sanitize PTY_FUZZ_CASES=2000 2>&1 | tee -a build/evidence/gates.txt

      - name: Record unavailable sanitizer gate
        if: ${{ matrix.sanitizer == false }}
        run: echo "sanitizer=unsupported-by-matrix" | tee -a build/evidence/gates.txt

      - name: Coverage-guided lexer fuzzing
        if: matrix.libfuzzer
        run: |
          set -o pipefail
          make fuzz-libfuzzer FUZZ_CC=clang FUZZ_SECONDS=30 2>&1 | tee -a build/evidence/gates.txt

      - name: Record unavailable libFuzzer gate
        if: ${{ matrix.libfuzzer == false }}
        run: echo "libfuzzer=unsupported-by-matrix" | tee -a build/evidence/gates.txt

      - name: PTY soak
        run: |
          set -o pipefail
          make CC=${{ matrix.cc }} soak SOAK_SECONDS=15 2>&1 | tee -a build/evidence/gates.txt

      - name: Record platform and latency evidence
        run: |
          set -o pipefail
          mkdir -p build/evidence
          uname -a > build/evidence/platform.txt
          ${{ matrix.cc }} --version >> build/evidence/platform.txt
          bash --version >> build/evidence/platform.txt
          zsh --version >> build/evidence/platform.txt
          make CC=${{ matrix.cc }} bench | tee build/evidence/latency.txt

      - name: Upload verification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: gsh-${{ matrix.label }}
          path: build/evidence
          if-no-files-found: error
